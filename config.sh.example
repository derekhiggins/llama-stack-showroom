# Configuration file template for llama-stack-platform
#
# This file is sourced by setup.sh and scripts from ~/.lls_showroom
# All values can be overridden by setting the environment variable before running scripts
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to your home directory:
#    cp config.sh.example ~/.lls_showroom
#
# 2. Edit ~/.lls_showroom and set required values:
#    o SHOWROOM_PULL_SECRET: base64-encoded quay.io credentials for setup.sh
#
# 3. Source the config before running scripts that need it:
#    source ~/.lls_showroom
#    python scripts/rag-demo.py <ARGS>
#


# REQUIRED: SHOWROOM_PULL_SECRET
#
# Instructions for getting a pull secret:
# 1. Go to quay.io and login
# 2. Top right -> Account Settings -> User Settings -> "CLI Password: Generate Encrypted Password"
# 3. Get a podman login command, e.g.:
#    podman login -u='higginsd' -p='A...A' quay.io
# 4. Update the command to add --authfile /tmp/ps.json:
#    podman login --authfile /tmp/ps.json -u='higginsd' -p='A...A' quay.io
# 5. Run the command, then extract the base64-encoded auth:
#    cat /tmp/ps.json | jq -r '.auths["quay.io"].auth'
# 6. Set SHOWROOM_PULL_SECRET to the output from step 5
export SHOWROOM_PULL_SECRET="${SHOWROOM_PULL_SECRET:-}"

# RHOAI catalog image and channel
# Available channels: alpha, beta, fast, fast-3.x, stable, stable-3.x, stable-3.3, eus-2.25, etc.
export SHOWROOM_CATALOG_IMAGE="${SHOWROOM_CATALOG_IMAGE:-quay.io/rhoai/rhoai-fbc-fragment:rhoai-3.4-ea.2}"
export SHOWROOM_OPERATOR_CHANNEL="${SHOWROOM_OPERATOR_CHANNEL:-beta}"


# Llama Stack custom image (optional - set to override default)
export SHOWROOM_LLAMA_STACK_IMAGE="${SHOWROOM_LLAMA_STACK_IMAGE:-}"

# Operator custom image (optional - set to override default)
export SHOWROOM_OPERATOR_IMAGE="${SHOWROOM_OPERATOR_IMAGE:-}"

# VLLM Inference configuration
export SHOWROOM_VLLM_URL="${SHOWROOM_VLLM_URL:-}"
export SHOWROOM_VLLM_API_TOKEN="${SHOWROOM_VLLM_API_TOKEN:-}"
export SHOWROOM_INFERENCE_MODEL="${SHOWROOM_INFERENCE_MODEL:-llama-3-2-3b}"

# VLLM Embedding configuration
export SHOWROOM_VLLM_EMBEDDING_URL="${SHOWROOM_VLLM_EMBEDDING_URL:-}"
export SHOWROOM_VLLM_EMBEDDING_API_TOKEN="${SHOWROOM_VLLM_EMBEDDING_API_TOKEN:-}"
export SHOWROOM_EMBEDDING_MODEL="${SHOWROOM_EMBEDDING_MODEL:-nomic-embed-text-v1.5}"
export SHOWROOM_EMBEDDING_PROVIDER_MODEL_ID="${SHOWROOM_EMBEDDING_PROVIDER_MODEL_ID:-nomic-ai/nomic-embed-text-v1.5}"
export SHOWROOM_EMBEDDING_DIMENSION="${SHOWROOM_EMBEDDING_DIMENSION:-768}"

# OpenAI API configuration (optional - for multi-agent demo)
export SHOWROOM_OPENAI_API_KEY="${SHOWROOM_OPENAI_API_KEY:-}"

# ======================================
# Deploy Local Changes Configuration
# ======================================
# Use ./scripts/deploy-local.sh to deploy local LlamaStack changes to the cluster
# See README.md for detailed documentation

# Path to local llama-stack repository
# Example: export LLAMA_STACK_SOURCE_PATH=~/projects/llama-stack
export LLAMA_STACK_SOURCE_PATH="${LLAMA_STACK_SOURCE_PATH:-}"

# Dev image namespace (namespace to store images in the in-cluster registry)
# Default: redhat-ods-applications (where LlamaStack runs)
export DEV_IMAGE_NAMESPACE="${DEV_IMAGE_NAMESPACE:-redhat-ods-applications}"

# Dev image name
export DEV_IMAGE_NAME="${DEV_IMAGE_NAME:-llama-stack-dev}"

# Dev image tag (auto-generated with timestamp if not set)
# export DEV_IMAGE_TAG="${DEV_IMAGE_TAG:-dev-$(date +%Y%m%d-%H%M%S)}"

# Base image for dev builds (auto-detected from operator if empty)
export DEV_BASE_IMAGE="${DEV_BASE_IMAGE:-}"

# Container tool to use (podman or docker)
export CONTAINER_TOOL="${CONTAINER_TOOL:-podman}"
