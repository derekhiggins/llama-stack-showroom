LlamaStack RHOAI components deployed.

{{- if .Values.route.enabled }}

LlamaStack API route:
  oc get route {{ .Values.llamastack.name }} -n {{ .Release.Namespace }} -o jsonpath='{.spec.host}'
{{- end }}

Sync secrets to local config for test.sh and demos:

  ./scripts/sync-secrets.sh
