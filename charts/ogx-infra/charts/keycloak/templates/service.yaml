apiVersion: v1
kind: Service
metadata:
  name: keycloak
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    {{- include "keycloak.selectorLabels" . | nindent 4 }}
