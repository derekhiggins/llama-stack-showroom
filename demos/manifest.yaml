# Manifest of runnable demos and tests
#
# Each entry specifies:
#   path: Relative path from project root
#   tags: List of tags for filtering (comma-separated or array)
#   name: Human-readable name
#   type: Execution type (python, shell, jupyter, etc.)
#   requires_key: Optional environment variable required to run
#
# Usage:
#   ./scripts/test.sh              # Run all demos
#   ./scripts/test.sh simple       # Run demos tagged 'simple'
#   ./scripts/test.sh rag,api      # Run demos tagged 'rag' OR 'api'
#   ./scripts/test.sh complex      # Run complex demos only

demos:
  - path: demos/hello/demo.py
    tags: [ci, fast, hello, basic]
    name: Hello World - Simple Chat Completion
    type: python
    description: Minimal example showing a single chat completion request

  - path: demos/rag/demo.py
    tags: [ci, rag, embeddings]
    name: RAG with Vector Search Demo
    type: python
    description: Demonstrates RAG with S3 file storage, embeddings, and vector search

  - path: demos/responses/demo.py
    tags: [ci, responses]
    name: OpenAI Responses API Demo
    type: python
    description: Verifies OpenAI SDK Responses API compatibility with multi-turn conversations

  - path: demos/tests/restarttest/restarttest.sh
    tags: [ci, test, persistence, integration]
    name: Response Persistence Test
    type: shell
    description: Verifies response persistence across pod restarts (requires oc cluster access)

  - path: demos/multi_agent/demo.py
    tags: [ci, agents, rag, responses, mcp]
    name: Multi-Agent Research Assistant Demo
    type: python
    requires_key: SHOWROOM_OPENAI_API_KEY
    description: Multi-agent system with triage routing to RAG, repository query, and general knowledge agents

  - path: demos/tests/abac_isolation/demo.py
    tags: [ci, test, abac, auth, security]
    name: ABAC Resource Isolation Test
    type: python
    description: Verifies ABAC ownership policies prevent cross-user resource access for vector stores, files, and responses

  # Agent demos - run via make run-cli with test input
  - path: agents/crewai/websearch_agent
    tags: [ci, agents, crewai]
    name: CrewAI Web Search Agent
    type: agent
    description: CrewAI agent with web search tool

  - path: agents/google/adk
    tags: [ci, agents, google, adk]
    name: Google ADK Agent
    type: agent
    description: Google Agent Development Kit based agent

  - path: agents/langgraph/human_in_the_loop
    tags: [ci, agents, langgraph]
    name: LangGraph Human-in-the-Loop Agent
    type: agent
    description: LangGraph agent with human approval workflow

  - path: agents/langgraph/react_agent
    tags: [ci, agents, langgraph]
    name: LangGraph ReAct Agent
    type: agent
    description: LangGraph ReAct pattern agent

  - path: agents/llamaindex/websearch_agent
    tags: [ci, agents, llamaindex]
    name: LlamaIndex Web Search Agent
    type: agent
    description: LlamaIndex agent with web search tool

  - path: agents/vanilla_python/openai_responses_agent
    tags: [ci, agents, vanilla]
    name: OpenAI Responses Agent
    type: agent
    description: Pure Python agent using OpenAI Responses API
